// Conversions

fn main() {
    var nums = 1234
    var floats = 1234.02
    var str_from_num = tostring(nums)
    var str_from_float = tostring(floats)

    println(str_from_num, typeof(str_from_num))
    println(str_from_float, typeof(str_from_float))
}